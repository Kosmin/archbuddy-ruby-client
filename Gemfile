# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# D47/M2: depend on the shared contract gem from the sibling engine repo via a
# local PATH source during development.
gem "architecture_auditor", path: "../architecture-auditor"
