# ─── SECRET: the de-anonymization key and any de-anonymized output MUST NEVER be committed ───
id-map.yml
*.id-map.yml
# de-anonymized exports contain real file/symbol names — treat as secret/local-only
report.yml
report.json
*.report.yml
*.report.json
*.dot
# de-anonymized OFFLINE html report (real symbols) — NOT the vendored
# cytoscape.min.js library asset under lib/, which is a committed runtime dep.
report.html
*.report.html
/out/
# the shared `.archbuddy/` workspace holds captured graph.yml + the SECRET
# id-map.yml + findings.yml — never stage it when dogfooding on this repo.
.archbuddy/

# Auditor I/O — captured graphs / findings from real repos
*.graph.yml
graph.yml
findings.yml
*.findings.yml
# v0.8 committed-cache aggregate — for AUDITED repos this is committed, but in
# the client's OWN repo (dogfood) it is a generated artifact, never source.
archbuddy-findings.json

# Bundler / gems
/.bundle/
/vendor/bundle/
/pkg/
*.gem
Gemfile.lock

# Ruby
/coverage/
/tmp/
/.yardoc/
/doc/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
